#include <iostream>
using namespace std;

int main() {
    int A, B, C, D, E;
    cin >> A;
    cin >> B >> C >> D >> E;

    cout << (A>B) << "\n" << (A>C) << "\n" << (A>D) << "\n" << (A>E);
    return 0;
}